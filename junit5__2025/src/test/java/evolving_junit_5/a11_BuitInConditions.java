package evolving_junit_5;

public class a11_BuitInConditions {
    // @Enabled / DisabledOnOs({LINUX, MAC, …})

    // @Enabled / DisabledOnJre({JAVA_11, …})

    // @Enabled / DisabledForJreRange(min = JAVA_9, max = JAVA_10)

    // @Enabled / DisabledIfSystemProperty(named = "someKey", matches = "someValue")

    // @Enabled / DisabledIfEnvironmentVariable(named = "SOME_KEY", matches = "SOME_VALUE")

    // @Enabled / DisabledIf("customCondition")

    // @Enabled / DisabledInNativeImage
}
